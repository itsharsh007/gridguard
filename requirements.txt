# GridGuard — CPU-only electricity theft detection
# Tested with Python 3.10–3.13

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
imbalanced-learn>=0.11
lightgbm>=4.0
shap>=0.44
matplotlib>=3.7
streamlit>=1.30
pyyaml>=6.0
joblib>=1.3
tqdm>=4.66

# Optional: uncomment the line below for neural models (1D-CNN, LSTM-Attention).
# Streamlit Community Cloud's 1 GB limit may not support PyTorch.
# For local / Render / larger cloud, install separately:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch>=2.1
